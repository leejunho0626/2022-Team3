<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.team3_project">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_baseline_camera_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Team3_Project">
        <activity android:name=".Start_Screen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity"/>
        <activity android:name=".User_Login"/>
        <activity android:name=".User_SignUp"/>
        <activity android:name=".Admin_Login"/>
        <activity android:name=".All_DataList"/>
        <activity android:name=".Login_Choice"/>

    </application>

</manifest>